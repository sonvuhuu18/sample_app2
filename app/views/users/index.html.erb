<% provide :title, t("all_users") %>
<h1><%= t("all_users") %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate %>
